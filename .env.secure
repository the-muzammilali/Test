# Frontend Configuration (Safe to expose)
VITE_API_BASE_URL=http://localhost:3001
VITE_API_KEY=chatbot-api-key-2024-secure-frontend-auth

# Note: Firebase credentials moved to secure backend
# Frontend now communicates through secure API only
# No sensitive credentials exposed to browser
